- defaults:
    link:
      relink: true

- clean: ['~']

- create:
    - ~/.config/keyboard/via
    # - ~/downloads
    # - ~/.vim/undo-history

- link:
    ~/.ideavimrc: ideavimrc
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig
    ~/.config/keyboard/via/keychron_k7_pro_layout.json: keychron_k7_pro_layout.json
    # lazygit
    # brew
    # zellij
    # fork
    # alfred
    # amethyst
    # appcleaner
    # android studio settings
    # xcode settings
    # sf symbols
    # obsidian
    # nvim (nvchad)

- shell:
#   - [git submodule update --init --recursive, Installing submodules]
  -
    command: ./setup_homebrew.zsh
    stdin: true
    stdout: true
    stderr: true
    description: Homebrew Setup
