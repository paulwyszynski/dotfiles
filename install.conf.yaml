- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.ideavimrc: ideavimrc
    ~/.zshrc: zshrc

# - create:
#     - ~/downloads
#     - ~/.vim/undo-history
#
# - shell:
#   - [git submodule update --init --recursive, Installing submodules]
