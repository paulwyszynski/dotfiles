- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.ideavimrc: ideavimrc
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig
    # lazygit
    # brew
    # zellij
    # fork
    # alfred
    # amethyst
    # appcleaner
    # android studio settings
    # xcode settings
    # sf symbols
    # obsidian
    # nvim (nvchad)

# - create:
#     - ~/downloads
#     - ~/.vim/undo-history
#
# - shell:
#   - [git submodule update --init --recursive, Installing submodules]
#   command: Run shell script here
#   stdin: trues
#   stdout: true
#   description: Bootstrapping tools
