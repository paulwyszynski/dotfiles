- defaults:
    link:
      relink: true

- clean: ['~']

- create:
    - ~/.config/keyboard/via
    - ~/Library/Application Support/lazygit
    # - ~/downloads
    # - ~/.vim/undo-history

- link:
    ~/.ideavimrc: idea-ide/ideavimrc
    ~/.zshrc: zsh/zshrc
    ~/.gitconfig: git/gitconfig
    ~/.config/keyboard/via/keychron_k7_pro_layout.json: qmk-via/keychron_k7_pro_layout.json
    ~/Library/Application Support/lazygit/config.yml: lazygit/config.yml
    # lazygit
    # brew
    # zellij
    # fork
    # alfred
    # amethyst
    # appcleaner
    # android studio settings
    # xcode settings
    # sf symbols
    # obsidian
    # nvim (nvchad)

- shell:
#   - [git submodule update --init --recursive, Installing submodules]
  -
    command: ./setup_homebrew.zsh
    stdin: true
    stdout: true
    stderr: true
    description: Homebrew Setup
